# Optional: enables Chinese editorial summaries and evidence-aware rescoring.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Optional fallback index. Enable openalex in config when a key is configured.
OPENALEX_API_KEY=

# Required only for an intentional enterprise WeChat delivery.
WECOM_WEBHOOK_URL=
